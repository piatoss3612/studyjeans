version: '3.9'
services:
  bot:
    build:
      context: .
      dockerfile: ./build/bot/Dockerfile
    image: bot
    container_name: bot
    restart: always
    environment:
      - BOT_TOKEN=<YOUR_BOT_TOKEN>
